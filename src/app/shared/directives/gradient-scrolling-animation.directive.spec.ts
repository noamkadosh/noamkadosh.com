import { GradientScrollingAnimationDirective } from './gradient-scrolling-animation.directive';

describe('GradientScrollingAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new GradientScrollingAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
