import { ScrollingAnimationDirective } from './scrolling-animation.directive';

describe('ScrollingAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollingAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
