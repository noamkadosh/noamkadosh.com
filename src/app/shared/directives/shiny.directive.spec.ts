import { ShinyDirective } from './shiny.directive';

describe('ShinyDirective', () => {
  it('should create an instance', () => {
    const directive = new ShinyDirective();
    expect(directive).toBeTruthy();
  });
});
