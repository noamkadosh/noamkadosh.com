import { ScrollingAnimation2Directive } from './scrolling-animation-2.directive';

describe('ScrollingAnimation2Directive', () => {
  it('should create an instance', () => {
    const directive = new ScrollingAnimation2Directive();
    expect(directive).toBeTruthy();
  });
});
