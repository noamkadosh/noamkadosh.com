import { HookDirective } from './hook.directive';

describe('HookDirective', () => {
  it('should create an instance', () => {
    const directive = new HookDirective();
    expect(directive).toBeTruthy();
  });
});
